<header>
  <section class="cabecera">
    <div class="pre-cabecera">
      <a href="#SobreMi"><span><strong>Sobre mi</strong></span></a>
      <img title="Javier Guerrero" src="/imgs/yo.webp" alt="Mi imagen de perfil" />
    </div>
    <div class="sub-cabecera">
      <a href="#Experiencia"><span><strong>Experiencia</strong></span></a>
      <a href="#Proyectos"><span><strong>Proyectos</strong></span></a>
      <a href="#Contacto"><span><strong>Contáctame</strong></span></a>
    </div>
  </section>
</header>

<style>
  header {
    font-size: 24px;

    position: sticky;
    top: 0;
    width: 100%;
    z-index: 10;

    animation: enhance-header linear both;
    animation-timeline: scroll(root);
    animation-range: 0 200px;
  }
  img {
    border-radius: 50%;
    height: 50px;
    opacity: 0;7
    box-shadow: 0 0 10px black;

    animation: enhance-img linear both;
    animation-timeline: scroll(root);
    animation-range: 0 450px;
  }
  .cabecera {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);

    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }
  .sub-cabecera {
    display: flex;
    gap: 32px;
    flex-wrap: wrap;
    margin-right: -14px;
  }
  .pre-cabecera {
    display: flex;
    gap: 16px;
    margin-left: -14px;
  }
  a {
    padding: 8px 14px;
  }
  @keyframes enhance-header {
    to {
      background: rgba(48, 48, 48, 0.25);
      backdrop-filter: blur(15px);
      font-size: 18px;
    }
  }
  @keyframes enhance-img {
    70% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
  @media (max-width: 725px) {
    .sub-cabecera {
      display: none;
    }
    img {
      animation-range: 0 0;
    }
    header {
      animation-range: 0 350px;
    }
  }
</style>
